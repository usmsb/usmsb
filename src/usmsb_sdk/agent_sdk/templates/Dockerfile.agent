# Agent SDK Dockerfile
# This Dockerfile creates a containerized agent that auto-registers with the platform

# Base image with the repository's pinned Python 3.14 runtime
FROM public.ecr.aws/docker/library/python:3.14-slim-bookworm@sha256:86f975aca15cf04a40b399eebede9aea7c82eae084d1f1a0a6ef6bcaae871a30 AS base

# Set environment variables
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

# Install system dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Create non-root user for security
RUN useradd --create-home --shell /bin/bash agent

# Set working directory
WORKDIR /app

# Copy requirements first for caching
COPY requirements-agent.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements-agent.txt

# Copy application code
COPY --chown=agent:agent . .

# Switch to non-root user
USER agent

# Expose default ports
# 8000: HTTP API
# 8765: WebSocket
# 9000: P2P
EXPOSE 8000 8765 9000

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

# Default command
CMD ["python3.14", "-m", "agent.main"]

# =====================================================
# Development Stage
# =====================================================
FROM base AS development

# Install development dependencies
USER root
RUN pip install --no-cache-dir \
    pytest \
    pytest-asyncio \
    pytest-cov \
    black \
    flake8 \
    mypy

USER agent

# Mount point for development
VOLUME ["/app"]

# Development command with auto-reload
CMD ["python3.14", "-m", "agent.main", "--reload"]

# =====================================================
# Production Stage
# =====================================================
FROM base AS production

# Production optimizations
ENV PYTHONOPTIMIZE=1

# Copy only necessary files
COPY --chown=agent:agent src/ /app/src/
COPY --chown=agent:agent config/ /app/config/
COPY --chown=agent:agent agent/ /app/agent/

# Production command with gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "--workers", "4", "--worker-class", "uvicorn.workers.UvicornWorker", "agent.main:app"]
