# Core
# Source-tree deployments must use the SDK from the same repository revision;
# the distributed runtime imports the physical-call telemetry contract added
# alongside it.  The independently published package keeps its normal version
# constraint in pyproject.toml and must only be released after that SDK version
# is published.
-e ..
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0

# LLM Runtime
vllm>=0.4.0
transformers>=4.35.0
torch>=2.0.0

# Utils
python-multipart>=0.0.6

# Dev
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-httpx>=0.28.0
